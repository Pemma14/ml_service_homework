# App Settings
APP__NAME="ML Service API"
APP__DEBUG=False
APP__MODE=PROD

# Security (CRITICAL: Change these in production!)
AUTH__SECRET_KEY=replace_this_with_a_secure_random_string_at_least_32_chars
SEED__ADMIN_EMAIL=admin@example.org
SEED__ADMIN_PASSWORD=replace_this_with_a_strong_password
SEED__DEMO_EMAIL=demo@example.org
SEED__DEMO_PASSWORD=replace_this_with_another_strong_password

# Database
DB__HOST=database
DB__PORT=5432
DB__NAME=ml_service
DB__USER=pema
DB__PASSWORD=my_super_password

# RabbitMQ
MQ__HOST=rabbitmq
MQ__PORT=5672
MQ__USER=mquser
MQ__PASSWORD=mqpassword

# Postgres (for docker-compose)
POSTGRES_USER=pema
POSTGRES_PASSWORD=my_super_password
POSTGRES_DB=ml_service

# RabbitMQ (for docker-compose)
RABBITMQ_DEFAULT_USER=mquser
RABBITMQ_DEFAULT_PASS=mqpassword

# Telegram Bot
BOT__TOKEN=your_telegram_bot_token_here
