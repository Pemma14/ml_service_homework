[project]
name = "psypharm-bot"
version = "0.1.0"
description = "Telegram bot for PsyPharmPredict service"
dependencies = [
    "aiogram>=3.0.0",
    "httpx>=0.25.0",
    "pydantic-settings>=2.0.0"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
